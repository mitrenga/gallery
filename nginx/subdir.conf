# nginx – several applications in subdirectories of one web root
# (https://media.example.com/gallery/, /masky/, /music/ …)
#
#   /var/www/media/gallery/   this project          -> https://media.example.com/gallery/
#   /var/www/media/masky/     a second gallery      -> https://media.example.com/masky/
#   /var/www/media/music/     music project         -> https://media.example.com/music/
#
# Each application protects its own static files with auth_request against its
# own auth.php – copy the "gallery" block for every further instance and change
# the directory name (the first URL segment) in the places marked (*).
#
# Install:  sudo cp nginx/subdir.conf /etc/nginx/sites-available/media.example.com
#           sudo ln -s ../sites-available/media.example.com /etc/nginx/sites-enabled/
#           sudo nginx -t && sudo systemctl reload nginx
# Then run the checklist from the README with BASE=https://media.example.com/gallery.

server {
	listen 80;
	listen [::]:80;
	server_name media.example.com;
	return 301 https://$host$request_uri;
}

server {
	listen 443 ssl http2;
	listen [::]:443 ssl http2;
	server_name media.example.com;

	root /var/www/media;
	index index.php;

	ssl_certificate     /etc/letsencrypt/live/media.example.com/fullchain.pem;
	ssl_certificate_key /etc/letsencrypt/live/media.example.com/privkey.pem;
	ssl_protocols TLSv1.2 TLSv1.3;
	add_header Strict-Transport-Security "max-age=63072000" always;

	# ---- never serve: passwords, git, scripts, docs, dot files (all applications) ----
	location ~ /config\.json$ { deny all; }
	location ~ /\.(?:git|ht) { deny all; }
	location ~* \.(?:sh|sql|log|sample|md)$ { return 404; }

	# ================= gallery  (/gallery/) =================
	location ~ ^/gallery/(?:gallery|thumbs)/ {                # (*)
		auth_request /auth-check-gallery;
	}
	location = /auth-check-gallery {
		internal;
		include fastcgi_params;
		fastcgi_pass unix:/run/php/php-fpm.sock;
		fastcgi_param SCRIPT_FILENAME $document_root/gallery/auth.php;   # (*)
		fastcgi_pass_request_body off;
		fastcgi_param CONTENT_LENGTH "";
	}

	# ================= masky – a second gallery instance  (/masky/) =================
	location ~ ^/masky/(?:gallery|thumbs)/ {
		auth_request /auth-check-masky;
	}
	location = /auth-check-masky {
		internal;
		include fastcgi_params;
		fastcgi_pass unix:/run/php/php-fpm.sock;
		fastcgi_param SCRIPT_FILENAME $document_root/masky/auth.php;
		fastcgi_pass_request_body off;
		fastcgi_param CONTENT_LENGTH "";
	}

	# ================= music  (/music/) =================
	# see nginx/subdir.conf in the music project (audio, covers, prepared downloads)
	location ~ ^/music/(?:cd|covers)/ {
		auth_request /auth-check-music;
	}
	location ~ ^/music/tmp/ {
		auth_request /auth-check-music;
		add_header Content-Disposition attachment;
	}
	location = /auth-check-music {
		internal;
		include fastcgi_params;
		fastcgi_pass unix:/run/php/php-fpm.sock;
		fastcgi_param SCRIPT_FILENAME $document_root/music/auth.php;
		fastcgi_param X_ORIGINAL_URI $request_uri;
		fastcgi_pass_request_body off;
		fastcgi_param CONTENT_LENGTH "";
	}

	# ---- the applications (SPA – /app/anything -> /app/index.php?anything) ----
	location / {
		try_files $uri $uri/ @rewrite;
		error_page 404 = @rewrite;
	}
	location @rewrite {
		rewrite ^/([^/]+)/(.*)$ /$1/index.php?$2 last;
	}

	location ~ \.php$ {
		try_files $uri =404;
		fastcgi_pass unix:/run/php/php-fpm.sock;
		fastcgi_index index.php;
		fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
		include fastcgi_params;
		fastcgi_read_timeout 300;
	}

	client_max_body_size 25m;

	gzip on;
	gzip_types text/plain text/css application/javascript application/json image/svg+xml;
}
